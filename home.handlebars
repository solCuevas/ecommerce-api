<!-- views/home.handlebars -->
<h1>Lista de productos (vista estática)</h1>
<ul>
  {{#each products}}
    <li>
      <strong>{{title}}</strong> - ${{price}} - {{category}}
    </li>
  {{/each}}
</ul>